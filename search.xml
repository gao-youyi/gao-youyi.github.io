<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>曲折的博客搭建</title>
    <url>/2021/04/11/%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA%E6%9C%89%E6%84%9F/</url>
    <content><![CDATA[<hr>
<image src="https://th.bing.com/th/id/R8017ed3b55c63759122c0a3e3355bb7c?rik=T4kvqRC2FH%2f%2f5A&riu=http%3a%2f%2fimg.wxcha.com%2ffile%2f201706%2f13%2faa6ae04b42.gif&ehk=p%2bgMFuX3kYlQtLn%2f8fYuecOmfvrWIIQIUpxsDUKQNjc%3d&risl=&pid=ImgRaw">
错误的打开方式
<hr>
没错，我这个苦逼青年，怀着无比的敬意，按照我亲爱的师哥得我发来的学习视频，从前天晚上8点开始，基本没有听下来的肝到了第二天早上四点多....
<h4>&nbsp &nbsp &nbsp然后就有了下面这样的我<h4>
<span id="more"></span>
<image src="https://ss.csdn.net/p?https://mmbiz.qpic.cn/mmbiz_png/ctwVEjeYQ2ic9QVvaUJoxwiaL1eFJ7iaKgcyR9PSledAYd9I6V7iaAlO8ibictfeZsRDSFCu4fOxChEvuNa1vpgJWz6Q/0?wx_fmt=png">
然后我这个苦逼的二货青年，不，是老年人，继续开始了挠头搔👂不知所以然的过程。当然，我也从中学会了很多的东西，比如坚持的力量，学会独处，总的来说，包涵以下几点：
<br>

<ul style="color:red">
<li>学习新的东西需要有勇于试错的决心</li>
<li>要敢于正视自己的困难，积极请教</li>
<li>要付出足够的时间和精力</li>
<li>要懂得享受着其中的乐趣</li>
<li>要学会一个人独处</li>
<li>保持谦逊</li>
</ul>
<!--more-->]]></content>
  </entry>
  <entry>
    <title>阶段性总结</title>
    <url>/2021/04/19/%E9%98%B6%E6%AE%B5%E6%80%A7%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<h1 id="阶段性总结"><a href="#阶段性总结" class="headerlink" title="阶段性总结"></a>阶段性总结</h1><h2 id="进入一路工作室之后的感想"><a href="#进入一路工作室之后的感想" class="headerlink" title="进入一路工作室之后的感想"></a>进入一路工作室之后的感想</h2><p>说实话，没想到这麽容易就进入了一路工作室，也就差不多学习了一个星期吧。写了一个CSDN页面，基于hexo搭建了一个博客。其他的真的就基本什么也没考核，这也让我深刻的意识到：学习和工作是自己的事情，如果没有自身的内在驱动，很难成就一番事业的。下一个阶段，我想把js学习了，然后开发一个网页端的学习笔记本。最后转为移动端。<br>进入工作室之后，我想充分利用工作室的资源，比如组队打比赛，和师哥们交流技术，积极的为自己获得实习资格….<br>同时学习了一些js我也再次意识到算法的重要性，从现在起，我就要重新正视算法，努力学习数据结构，同时关于计算机基础的知识也要进一步扩充了。</p>
<h2 id="机会总是留给有准备的人的"><a href="#机会总是留给有准备的人的" class="headerlink" title="机会总是留给有准备的人的"></a>机会总是留给有准备的人的</h2><p>上学期为了加入星三好工作室，学习了很多的前端基础，所以这学期才会更加顺利的完成一路给我的任务。很多事情都是这样，你永远也不知道你以后会做什么，遇见什么事情，你现在比别人1多学习的东西，都有可能帮助到你。所以，加油，我的少年！！！</p>
]]></content>
      <categories>
        <category>about</category>
      </categories>
  </entry>
  <entry>
    <title>这就是我</title>
    <url>/2021/04/19/%E4%B8%AA%E4%BA%BA%E6%83%85%E6%84%9F/Readme/</url>
    <content><![CDATA[<h2 id="Do-you-know-me"><a href="#Do-you-know-me" class="headerlink" title="Do you know me?"></a>Do you know me?</h2><p>13414</p>
]]></content>
  </entry>
  <entry>
    <title>css定位</title>
    <url>/2021/04/18/%E5%89%8D%E7%AB%AF%E5%BC%80%E5%8F%91/%E5%AE%9A%E4%BD%8D/</url>
    <content><![CDATA[<h1 id="定位学习手册"><a href="#定位学习手册" class="headerlink" title="定位学习手册"></a>定位学习手册</h1><h2 id="定位-定位模式-边偏移"><a href="#定位-定位模式-边偏移" class="headerlink" title="定位=定位模式+边偏移"></a>定位=定位模式+边偏移</h2><blockquote>
<p>定位模式：指定一个元素在文档中的定位方式<br>边偏移：决定元素的最终位置</p>
</blockquote>
<h3 id="四个position属性"><a href="#四个position属性" class="headerlink" title="四个position属性:"></a>四个position属性:</h3><blockquote>
<blockquote>
<blockquote>
<p>static:了解就行<br>1，无定位，和标准流一样。<br>relative：重要！<br>1，它的边偏移是相对自己原来的位置来说的。<br>2，即使移动了，自己原来的位置继续占有，不像浮动一样脱标！<br>absolute:异常重要！！<br>1,如股没有限制，相对于最大的盒子body;<br>2如果父级元素存在任意一种(除了static)定位，则以最近一级父级(也可以是爷爷级别元素)盒子作为参考点移动;否则还是以body作为定位；<br>3,绝对定位的盒子位置不会保留，像浮动型，脱离标准流；<br>fixed:重要<br>1，元素的位置不会随着页面的滚动而动，参照点是以浏览器的可视窗口来决定；</p>
</blockquote>
</blockquote>
</blockquote>
<h3 id="子绝对父相对"><a href="#子绝对父相对" class="headerlink" title="子绝对父相对"></a>子绝对父相对</h3><p>一般情况下，如果孩子是absolute定位，父亲采用relative定位，来限制孩子的位置；</p>
<h2 id=""><a href="#" class="headerlink" title=""></a></h2>]]></content>
      <tags>
        <tag>前端开发</tag>
      </tags>
  </entry>
  <entry>
    <title>单链表的实现和基本函数操作</title>
    <url>/2021/04/23/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E9%A1%BA%E5%BA%8F%E8%A1%A8/%E5%8D%95%E9%93%BE%E8%A1%A8%E7%9A%84%E5%AE%9E%E7%8E%B0%E5%92%8C%E5%9F%BA%E6%9C%AC%E5%87%BD%E6%95%B0%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<pre><code>---C++
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
/链表的实现和函数实现
/*链表的结构必定包涵一个指针域，所以地址的分配不一定连续
由计算据随机分配，所以单链表只能单向操作，但是也正是因此
可以使得表基本无线扩容。 
结构：
每个数据元素由结点构成
链表的第一个元素节点称为首结点 
最后一个元素称为尾结点，指针域NULL
一般定义head（头节点）指向首结点a1，这样比较好！！！ 
*/
</code></pre>
<span id="more"></span>
<pre><code>typedef int ElemType;
typedef struct node
&#123;
ElemType data;       //数据域
struct node *next; //指针域
&#125; LNode, *LinkList;

//初始化单链表
int InitList_L(LinkList &amp;L)
&#123;
L = (LinkList)malloc(sizeof(LNode)); //分配空间
if (!L)
    return 0;    //判断是否成功
L-&gt;next = NULL; //初始结点指针域为空
return 1;
&#125;

//销毁单链表
int DestroyList(LinkList &amp;L)
&#123;
LinkList p;
while (L)
&#123;
    p = L-&gt;next;
    free(L);
&#125;
&#125;

//求单链表的长度
int ListLength(LinkList L)
&#123;
int i = 0;
//不能直接while(L)因为这样会把头节点移动到尾部；
LinkList p = L-&gt;next;
while (p)
&#123;
    i++;
    p = p-&gt;next;
&#125;
return i;
&#125;

//求单链表中值为e的元素
int LocataElem(LinkList L, ElemType e)
&#123;
int i = 0;
LinkList p = L-&gt;next;
while (p)
&#123;
    i++;
    if (p-&gt;data == e)
        return i;
    p = p-&gt;next;
&#125;
return 0; //如果没有找到，返回0；
&#125;

//单链表的插入操作
int ListInsert_L(LinkList &amp;L, int i, ElemType e)
&#123;
LinkList p, s; //创建两个指向结点的指针，这里的LinkList等价于LNode * ；
p = L;           //让p指向头节点；
int j = 0;
while (p &amp;&amp; j &lt; i - 1)
&#123; //先让p找到目标结点的上一个结点，
    p = p-&gt;next;
    ++j;
&#125;
if (!p || j &gt; i - 1)
    return 0; //判断i是否合法
//给s分配空间，来存储数据e，
s = (LinkList)malloc(sizeof(LNode));
s-&gt;data = e;

/*先让s的指针域指向i,此时i-1对应的结点，此时s和p
都指向目标结点*/
s-&gt;next = p-&gt;next;

/*再让p指向s，这样就把s插入到目标结点的前面了，也
就是把e插入进去了，此时链表i位置上就是e了；*/
p-&gt;next = s;
return 1;
&#125;

//单链表的删除算法,类似于插入操作
int ListDelete_L(LinkList &amp;L, int i, ElemType &amp;e)
&#123;
LinkList p, q;
p = L;
int j = 0;
//同样，先找到i-1个位置
while (p-&gt;next &amp;&amp; j &lt; i - 1)
&#123;
    p = p-&gt;next;
    ++j;
&#125;
//判断上一步是否正确进行
if (!p-&gt;next || j &gt; i - 1)
&#123;
    return 0;
    ;
&#125;
/*这里要先让p指向q,再让q指向p-&gt;next，
也就是把链表通过q链接起来，再让p指向
q的下一个位置，此时p就直接跳过i结点，指向
i+1结点了，这时候就可以释放i结点了/
如果直接让q指向i+1的结点，那么i的结点就会被遗忘在
缓存区，当程序运行足够长时间时，会导致内存不够，
或者其他的关于内存的问题*/
q = p-&gt;next;
p-&gt;next = q-&gt;next;
e = q-&gt;data;
free(q);
return 1;
&#125;

void ShowAddress(LinkList L)
&#123;
int i;
LinkList p = L-&gt;next;
//当然，这里如果使用while写起来会更简便一些
if (p)
&#123;
    printf(&quot;\n头节点的地址为：\t%p\n&quot;, L); //前面说过了，头结点一般不存储数据
    for (i = 1; p; i++)
    &#123;
        printf(&quot;第%d个结点的值为：\t%d;\t地址为：%p\n&quot;, i, p-&gt;data, p);
        p = p-&gt;next;
    &#125;
&#125;
&#125;

int main()
&#123;
int A[8] = &#123;1, 2, 3, 4, 5, 100, 200, 300&#125;; //测试数据
int i, j, e = 586;                           //e时是测试数据
LinkList head, p;                           //head是头节点，相当于&amp;L;
InitList_L(head);

for (i = 1, j = 0; i &lt;= 8; i++, j++) //注意这里i从1开始，
    ListInsert_L(head, i, A[j]);     //将数据依次放入

printf(&quot;\n\n 当前单链表的状态为：\n&quot;);
ShowAddress(head);
i = 6; //把e放在第六个位置；
printf(&quot;\n \ n 把e = %d 插入到第 %d 个位置后，单链表的状态为：\n&quot;, e, i);
ListInsert_L(head, i, e);
ShowAddress(head); //查看此时各个元素的位置和数据
i = 5;
//测试删除操作
printf(&quot;\n \ n 删除第 %d 结点后的单链表状态： \n &quot;, i);
ListDelete_L(head, i, e);
ShowAddress(head); //展示删除成果
&#125;
...
</code></pre>
<h3 id="致歉："><a href="#致歉：" class="headerlink" title="致歉："></a>致歉：</h3><blockquote>
<p>非常抱歉各位，昨天写代码的时候没有及时的检查和测试，导致程序出现问题，我表示深刻反思。</p>
</blockquote>
]]></content>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>顺序表学习笔记</title>
    <url>/2021/04/21/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E9%A1%BA%E5%BA%8F%E8%A1%A8/%E9%A1%BA%E5%BA%8F%E8%A1%A8%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<pre><code>&#39;&#39;&#39;C++
#include &lt;stdlib.h&gt;
#include &lt;stdio.h&gt;
//循序表实例
/* 逻辑顺序和物理顺序一致，连续存储，可以任意访问*
类型声名如下：*/
/*静态存储*/
#define LIST_INIT_SIZE 100 //存储空间的初始分配量,这是顺序存储的缺点
#define LIST_INCREMENT 10  // 存储空间的分配增量
typedef int ElemType;      //存储元素的数据类型，可以自定义
</code></pre>
<span id="more"></span>
<pre><code>typedef struct
&#123;
ElemType *elem; //存储空间的基地址
int length;     //当前长度
int listsize;   // 当前分配的存储容量
&#125; SqList;

//初始化线性表
int InitList_Sq(SqList &amp;L)
&#123;
//构造一个空的先线性表
L.elem = (ElementType *)malloc(LIST_INIT_SIZE * sizeof(ElementType)); //分配空间
//测试分配是否成功
if (!L.elem)
    return 0;
//初始化
L.length = 0;                //初始数据量为0
L.listsize = LIST_INIT_SIZE; //初始存储量为最大
return 1;
&#125;

//销毁线性表:动态分配必须释放
int DestoryList(SqList &amp;L)
&#123;
free(L.elem); //由INIT函数可以知道，此处可以直接一步释放
//释放后不要忘记修改参数的值
L.elem = NULL;
L.length = 0;
L.listsize = 0;
return 1;
&#125;

//求线性表的长度
int GetLength(SqList L)
&#123;
return L.length; //length里面记录了表中元素的个数
&#125;

//求线性表中第i个元素
int GetElem(SqList L, int i, ElemType &amp;e)
&#123;
//注意进行数据i是否合法的判断
if (i &lt; 1 || i &gt; L.length)
&#123;
    return 0;
&#125;
//返回对应的值
e = *(L.elem + i - 1); //注意线性表中第i个元素的下标为i-1,下标为i的元素的下标为i+1
return 1;
&#125;

//按值查找求下标
ElemType LocateElem(SqList L, ElemType e)
&#123;
ElemType *p;
int i = 1;
p = L.elem;
//循环遍历，比较查找
while (i &lt; L.length &amp;&amp; (*p++ != e))
&#123;
    i++;
&#125;
//判断返回值是否合理
if (i &lt;= L.length)
&#123;
    return i;
&#125;
else
&#123;
    return 0;
&#125;
&#125;

//插入数据
int ListInsert(SqList &amp;L, ElemType e, int i)
&#123;
/*插入算法：
first,将新元素e插入到L中逻辑序号为i的位置，对应于L.elem[i-1]
second,判断i的值是否合法（1&lt;=i&lt;=L.length+1）
third,second满足时，将L.elem[i-1...length-1]都后移一个位置
然后在L.elem[i-1]处插入e,然后对表中相关参量进行修改。*/
//这里先使用一种特别笨的算法进行插入操作，后续进一步学习会介绍更加简洁的算法，但是每一种算法的实现都要掌握

//判断i是否合法
ElemType *p;
if (i &lt; 1 || i &gt; L.length + 1)
&#123;
    return 0;
&#125;
//这是一个拓展功能，使得数据无论如何都可以呗被存储。可有可无，有的学校不讲
if (L.length &gt;= L.listsize)
&#123; //realloc函数自带释放原有空间的功能
    ElemType *newbase = (ElemType *)realloc(L.elem,
                                            (L.listsize + LIST_INCREMENT) * sizeof(ElemType));
    if (!newbase)
    &#123; //判断是否拓展成功
        return 0;
    &#125;
    L.elem = newbase;
    L.listsize += LIST_INCREMENT; //修改表中相关参数的值
&#125;
ElemType *q = &amp;(L.elem[i - 1]);                //q为插入的位置
for (p = &amp;(L.elem[L.length - 1]); p &gt;= q; --p) //先让p指向最后一个位置，然后一直遍历到q,
&#123;
    *(p + 1) = *p; //等价于p--;
    *q = e;        //让新的元素e插入到第i个位置，也就是数组的i-1处。
    ++L.length;    //修改表中的相关参数
    return 1;
&#125;
&#125;

//删除某个位置的元素
int ListDelete_Sq(SqList &amp;L, int i, ElemType &amp;e)
&#123;
/*
1，删除顺序表中逻辑序号为i的元素
2，判断输入i是否合法
3，当2生效时，将L.elem[i.......length-1]的每一个
元素都前移一个位置，然后修改表中的相关参数。
*/
ElemType *p, *q;
if (i &lt; 1 || i &gt; L.length)
&#123; //step 1,判断
    return 0;
&#125;
p = &amp;(L.elem[i - 1]);      //step 2，定位
e = *p;                    //不是必须的
q = L.elem + L.length - 1; //表末尾的位置
for (++p; p &lt;= q; ++p)
&#123; //step 3,修改
    *(p - 1) = *p;
&#125;
--L.length; //重要！！
return 1;
&#125;

//显示当前表中的数据
void display(SqList L)
&#123;
for (int i = 1; i &lt;= L.length; i++)
&#123;
    printf(&quot;%d&quot;, L.elem[i - 1]);
&#125;
&#125;

int main()
&#123;
int A[8] = &#123;1, 2, 3, 4, 5, 100, 200, 300&#125;;
int i, j, e = 586;
SqList List;
Init(List);
//插入初始数据
for (i = 1, j = 0; i &lt;= 8; i++, j++)
    ListInsert(List, i, A[j]);
printf(&quot;\n 插入之前的元素序列为：\n&quot;);
display(List);
i = 3; //插入的位置
ListInsert(List, i, e);
print(&quot;\n\n插入后的元素序列为：\n&quot;);
display(List);
i = 6; //删除的位置
printf(&quot;\n\n删除后的元素序列为;\n&quot;);
ListDelete_Sq(List, i, e);
display(List);
&#125;
&#39;&#39;&#39;
</code></pre>
]]></content>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>自动增长的数组</title>
    <url>/2021/04/17/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E9%A1%BA%E5%BA%8F%E8%A1%A8/%E8%87%AA%E5%8A%A8%E5%A2%9E%E9%95%BF%E7%9A%84%E6%95%B0%E7%BB%84/</url>
    <content><![CDATA[<h1 id="可以自动增长的数组"><a href="#可以自动增长的数组" class="headerlink" title="可以自动增长的数组"></a>可以自动增长的数组</h1><h2 id="纯粹的数组实现："><a href="#纯粹的数组实现：" class="headerlink" title="纯粹的数组实现："></a>纯粹的数组实现：</h2><pre><code>---C语言 
</code></pre>
<p>//可增长的数组<br>//初级版本，没有加入数据结构1的思想，后续有新的版本<br>//参考资料：浙大翁恺的C语言进阶 </p>
<span id="more"></span>
<p>//#include&lt;stdio.h&gt;<br>//#include&lt;stdlib.h&gt;</p>
<!--more-->
<p>typedef struct {<br>    int <em>array;<br>    int size;<br>}Array;//最好不要写</em>Array，虽然方便，但后续很麻烦 </p>
<p>Array array_creat(int init_size){// 不返回指针目的是避免潜在的危险<br>    Array a;//只是创建本地变量，而不是指针。<br>    a.size = init_size;<br>    a.array = (int*)malloc(sizeof(int)*a.size);<br>    return a;//<br>}//创建数组 </p>
<p>void arry_free(Array *a){<br>    free(a-&gt;array) ;<br>    a-&gt;array = NULL;<br>    a-&gt;size = 0;</p>
<p>}//回收数组的空间 </p>
<p>//封装 ,保护数据<br>//告诉我们数组中有多少个单元可以用<br>int array_size(const Array *a){<br>    return a-&gt;size;<br>}</p>
<p>//访问数组的某个单元<br>//以指针类型返回该位置，方便后续的对该点的赋值。<br>int *array_at(Array *a,int index){<br>    if(index &gt;= a-&gt;size){<br>        array_inflate(a, (index-a-&gt;size)+1);<br>/<em>此处有很多种方法，有一种“块”（block）思想在后续的文档中有解释</em>/<br>    };<br>    return  &amp;(a-&gt;array[index]) ;<br>}</p>
<p>//使得数组长大<br>//所谓的增大其实显然是不可能在a的直接实现<br>//方法就是先创建一块新的空间P，先将a的数据保存到新的空间P<br>//然后让空的a指向P,此时相当于a实现了自我长大。<br>//再将旧的空间释放， </p>
<p>void array_inflate(void array_inflate){<br>    int <em>p = (int</em>)malloc(sizeof(int)(a-&gt;size+more_size));<br>    int i;<br>    for(i=0; i<a->size;i++){//数据保存<br>        p[i] = a-&gt;array[i];<br>    }<br>    free(a-&gt;array);//释放旧的a<br>    a-&gt;array = p;//长大了<br>    a-&gt;size += more_size;<br>    }</p>
<p>int main(int argc,char const*argv[]){<br>    Array a = array_creat(100);<br>    *array_at(&amp;a,1000) = 1000;<br>    printf(“数组的大小：%d\n”,array_size(&amp;a));<br>    printf(“该点的值是：%d\n”,*array_at(&amp;a,0));<br>    return 0 ;<br>} </p>
<pre><code>---
</code></pre>
]]></content>
  </entry>
  <entry>
    <title>头插法和尾插法解释</title>
    <url>/2021/04/23/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E9%A1%BA%E5%BA%8F%E8%A1%A8/%E5%A4%B4%E6%8F%92%E6%B3%95%E5%92%8C%E5%B0%BE%E6%8F%92%E6%B3%95%E8%A7%A3%E9%87%8A/</url>
    <content><![CDATA[<h3 id="由于书本上对于头插法和尾插法已经解释的很好了，我这里直接放上链接，大家需要的可以自己查看。"><a href="#由于书本上对于头插法和尾插法已经解释的很好了，我这里直接放上链接，大家需要的可以自己查看。" class="headerlink" title="由于书本上对于头插法和尾插法已经解释的很好了，我这里直接放上链接，大家需要的可以自己查看。"></a>由于书本上对于头插法和尾插法已经解释的很好了，我这里直接放上链接，大家需要的可以自己查看。</h3><p><a href="https://blog.csdn.net/qq_41028985/article/details/82859199?ops_request_misc=%257B%2522request%255Fid%2522%253A%2522161923112716780269869000%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D&request_id=161923112716780269869000&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~top_positive~default-1-82859199.first_rank_v2_pc_rank_v29&utm_term=%E5%A4%B4%E6%8F%92%E6%B3%95">点击链接查看外部解释</a></p>
]]></content>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
</search>
